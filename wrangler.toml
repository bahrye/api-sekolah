name = "api-sekolah-kita"
pages_build_output_dir = "./"

# Database utama: Supabase (PostgreSQL)
# Konfigurasi: SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY di .dev.vars (lokal) & Cloudflare Pages (produksi)