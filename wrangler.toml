name = "api-sekolah-kita"
pages_build_output_dir = "./"

# Database utama: Neon PostgreSQL
# Set DATABASE_URL di Cloudflare Pages → Settings → Environment variables
# Lokal: salin ke .dev.vars (lihat .dev.vars.example)