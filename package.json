{
  "name": "api-sekolah",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy:cf": "wrangler deploy -c wrangler.cron.toml && wrangler pages deploy . --project-name=api-sekolah-kita --branch main --commit-dirty=true",
    "deploy": "npm run deploy:cf",
    "ship": "node scripts/ship.js",
    "ship:all": "node scripts/ship.js && npm run deploy:cf"
  },
  "devDependencies": {
    "wrangler": "^4.92.0"
  }
}
